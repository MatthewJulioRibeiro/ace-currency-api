Place the "IBM App Connect Enterprise for Developers" Linux x86_64 download here as `ace-developer.tar.gz` before running `docker build`.

Download it (free registration, non-commercial/dev-use license — see the main README) from IBM Fix Central, searching for "IBM App Connect Enterprise for Developers".

This file is intentionally not committed to the repository (proprietary binary, ignored via `.gitignore`).
